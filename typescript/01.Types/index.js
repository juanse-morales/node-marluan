"use strict";
// Types
var framework = "Angular";
var isProduction = true;
var PORT = 8080;
var styles = [1, 'Texto'];
var styles2 = ['1', 'Texto'];
var sayHello = function (name) { return "Hello, " + name; };
var value = sayHello('Sara');
console.log(value);
